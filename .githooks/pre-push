#!/usr/bin/env bash

make clean

make test comment-edit-test-readme > /dev/null 2>&1
err=$?

if [[ $err -gt "0" ]]; then
    echo "[ERROR] The README.md must be generated from commentary!"
fi

make clean
exit $err
